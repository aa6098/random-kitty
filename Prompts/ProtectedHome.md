Create a home page in (Protected) folder which only logged user can navigate.
The page display data from member table.
Each member need to be displayed on card.
The member card should contain image field from Member table.
The displayName from Member table need to be displayed on the card in Bold and larger font.
The location Name from corresponding location table joining LocationId need to be displayed below it.
Use ShadCN components.
Retrieve member data in Server Side component.
The card need to be Navigation link, which will navigate to a page which we will create later.